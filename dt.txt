RBAC System Static vs Dynamic Data
permission          <-- static
role                <-- static
role_permission     <-- static
The static data is generated by seed permissions script
user_organization   <-- dynamic
user_project        <-- dynamic


RBAC Architecture
User
 │
 ├── UserOrganization
 │       │
 │       ▼
 │      Role (Admin, Owner etc)
 │       │
 │       ▼
 │  RolePermission (with Role_id find out the relevant permission IDs)
 │       │
 │       ▼
 │   Permission (Using permission Ids find the relevant permissions)
 │
 └── UserProject
         │
         ▼
        Role (Engineer, QA ETC)
         │
         ▼
    RolePermission (with Role_id find out the relevant permission IDs)
         │
         ▼
     Permission (Using permission Ids find the relevant permissions)


Adding a new user only inserts into UserOrganization or UserProject.
Changing a user's role only updates the role_id in those tables.
Adding a new permission only updates the global RolePermission table.
No duplication of roles or permissions across organizations.


# The Architecture for your project is below
# HTTP Request
#      │
#      ▼
# Pydantic Request Schema
#      │
#      ▼
# Service
#      │
#      ▼
# SQLAlchemy Model
#      │
#      ▼
# Database
#      │
#      ▼
# SQLAlchemy Model
#      │
#      ▼
# Pydantic Response Schema
#      │
#      ▼
# HTTP Response


Organization Module
        │
        ▼
Project Module
        │
        ▼
Ticket Module
        │
        ▼
Kanban Board Module


Once the core product is stable, I'd prioritize enhancements in roughly this order:

Notifications 
Email
In-app
Slack/Webhooks
Dashboard & Analytics
Burndown chart

Velocity
Ticket counts
Lead/Cycle time
Labels & Filtering
Labels
Saved filters

Advanced search
Subtasks & Dependencies
Parent-child tickets
Blocked-by / blocks relationships
Sprint Management
Backlog
Sprint planning
Active sprint
Sprint reports
Automation

Rules like "When ticket moves to Done, notify reporter"
Due date reminders
Status transition automations
Knowledge Base
Project documentation
Wiki pages
Runbooks


SQL QUERY TO GET OWNER AND ADMINISTRATOR DETAILS FOR Organization USER
SELECT
    o.id   AS organization_id,
    o.name AS organization_name,
    u.id   AS user_id,
    u.name AS user_name,
    u.email,
    r.name AS role
FROM app.organization o
JOIN app.user_organization uo
    ON uo.organization_id = o.id
JOIN app."user" u
    ON u.id = uo.user_id
JOIN app.role r
    ON r.id = uo.role_id
WHERE r.name IN ('Owner', 'Administrator')
ORDER BY
    o.name,
    CASE
        WHEN r.name = 'Owner' THEN 1
        ELSE 2
    END,
    u.name;


SQL QUERY TO GET Project Owner DETAILS FOR project USER
SELECT
    p.id   AS project_id,
    p.name AS project_name,
    u.id   AS user_id,
    u.name AS user_name,
    u.email,
    r.name AS role
FROM app.project p
JOIN app.user_project up
    ON up.project_id = p.id
JOIN app."user" u
    ON u.id = up.user_id
JOIN app.role r
    ON r.id = up.role_id
WHERE r.name IN ('Project Owner', 'Project Administrator')
ORDER BY
    p.name,
    CASE
        WHEN r.name = 'Project Owner' THEN 1
        ELSE 2
    END,
    u.name;


SELECT
    o.id   AS organization_id,
    o.name AS organization_name,
    p.id   AS project_id,
    p.name AS project_name,
    u.id   AS user_id,
    u.name AS user_name,
    u.email,
    r.name AS role
FROM app.project p
JOIN app.organization o
    ON o.id = p.organization_id
JOIN app.user_project up
    ON up.project_id = p.id
JOIN app."user" u
    ON u.id = up.user_id
JOIN app.role r
    ON r.id = up.role_id
WHERE r.name = 'Project Admin'
ORDER BY o.name, p.name, u.name;


WEBSOCKET ENDPOINT FOR TESTING
ws://127.0.0.1:8000/ws/notifications?token=<token>

Install cloudflare tunnel on your machine
run the below command
cloudflared tunnel --url http://localhost:5173

you will get a temporary domain
you can test your application


ROADMAP AHEAD
1) STATE RULE MACHINE
2) DEVELOPER ANALYTICS PAGE
3) PRICING PAGE AND PRICING BASED RESTRICTIONS
4) RATE LIMITER
5) REDIS BASED PERMISSION LOADING FOR users
6) TESTING REQUIRED PERMISSIONS FOR ALL API
7) NOTIFICATION MODULE SUPPORTING MAIL TEAMS SLACK AND IN APP NOTIFICATION
8) NOTIFICATIONS SHOULD BE RELIABLE AND RETRIABLE AND REPLAYABLE
9) REDIS AND CELERY SETUP AND EFFECTIVE USE
10) TICKET LISTENER FEATURE
11) 